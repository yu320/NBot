好的，這是一份為您的 NBot 專案量身打造的**終極開發指南**。


1.  **如何根絕 `.png` 中的多重錯誤訊息** (注意事項 1)。
2.  **如何正確建立 `HybridCommand` (混合指令)**，包含 `#` (公開) 與 `/` (私人) 的回覆SOP (注意事項 2)。
3.  **如何修正 `/monitor` 無法顯示的 `AttributeError`** (注意事項 2 - 範例 D)。
4.  **如何將舊程式碼遷移到新標準** (注意事項 3)。
5.  **您要求的新功能範例**：如何讀取 JSON (例如 FAQ) (注意事項 4)。
6.  **您要求的新功能範例**：如何建立關鍵字自動回應 (包含文字、表情符號、圖片) (注意事項 5)。



-----

# NBot (宿宿好幫手) 協作開發指南 V3.0 (最終完整版)

歡迎加入 NBot 的開發！為確保程式碼品質一致，並避免先前遇到的「重複錯誤回覆」和「指令回覆混亂」問題，請所有協作者遵守以下核心指南。

## ⭐️ 核心原則

1.  **Cogs 獨立 (最重要)**：每個 Cog (功能模組) **絕對不能**處理不屬於自己的錯誤。
2.  **Hybrid 優先**：所有指令**必須**使用「混合指令」(`Hybrid Command`)，以同時支援 `#` (前綴) 和 `/` (斜線)。
3.  **動態回覆 (SOP)**：所有回覆**必須**遵守「`/` 私人, `#` 公開」的原則。

-----

## ⚠️ 注意事項 1：【必讀】如何根絕「多重/重複錯誤訊息」

**問題**：當 `#addevent` 出錯時，`calendar.py` 的錯誤監聽器會啟動，**但 `main.py`, `musiclist.py`, `music_play.py` 的監聽器也會被觸發**。它們發現 `addevent` 不屬於自己，就進入 `else` 區塊，呼叫了 `bot.py` 的全域錯誤處理器，導致您看到一堆「未知的內部錯誤」。

**規則**：**所有** `cmds/` 資料夾中的 `on_command_error` 函式，都**必須**使用以下範本。

這**是 `.png` 錯誤的**主因\*\*。

**問題分析**：
當一個指令（如 `#addevent`）出錯時，**所有**的 Cogs 檔案（`main.py`, `calendar.py`, `music.py`...）中的 `on_command_error` 監聽器**都會被觸發**。

  * `calendar.py` 發現是自己的錯，於是回覆了「⚠️ 參數遺漏錯誤」。
  * 但 `main.py` 發現這**不是** `ping` 或 `clean`，於是它進入了 `else` 區塊。
  * 這個 `else` 區塊（您指出的 `self.bot.on_command_error(ctx, error)`）會**手動將錯誤再次丟給 `bot.py`** 的全域錯誤處理器。
  * 結果：`bot.py` 收到了來自 `main.py`, `musiclist.py`, `music_play.py` 等**多個 Cog** 丟上來的同一個錯誤，導致您看到**數次的**「❌ 發生了一個未知的內部錯誤」。

**解決方案**：
**所有** Cogs 的 `on_command_error` 函式，都**必須**在開頭加入「Cog 名稱檢查」，並且**必須刪除**那個 `else` 區塊。

### ✅ 正確的 `on_command_error` 範本 (適用所有 Cogs)

```python
    @commands.Cog.listener()
    async def on_command_error(self, ctx, error):
        
        # ✅ 1. 關鍵修正：檢查此錯誤是否來自本 Cog
        # (將 'MyCogName' 替換為該 class 的名稱, 例如 'Main', 'Calendar', 'MusicPlay')
        if ctx.command and ctx.command.cog_name != 'MyCogName':
            return # 錯誤不屬於我，立刻退出，不處理

        # (日誌只會記錄本 Cog 的錯誤)
        logging.warning(f"MyCogName Cog 捕獲到指令錯誤 (指令: {ctx.command}, 錯誤: {error})")

        # ✅ 2. 檢查是否為私人回覆
        is_private = ctx.interaction is not None
        
        # ✅ 3. 處理本 Cog 自己的錯誤 (例如 'play' 指令)
        if ctx.command and ctx.command.name in ['play', 'p']:
            
            # --- 處理「遺漏參數」 ---
            if isinstance(error, commands.MissingRequiredArgument):
                # (保留您要的完整範例)
                await ctx.send(
                    f"⚠️ **您忘記提供歌曲名稱或連結了！**\n\n"
                    f"**👉 正確格式：**\n"
                    f"`{ctx.prefix}{ctx.command.name} [YouTube 關鍵字或 URL]`\n"
                    f"**範例：** `{ctx.prefix}{ctx.command.name} Never Gonna Give You Up`",
                    ephemeral=is_private # ✅ 使用動態回覆
                )
            
            # --- 處理「權限不足」 ---
            elif isinstance(error, commands.MissingPermissions):
                await ctx.send("❌ **權限不足：** 您沒有權限執行此指令。", ephemeral=is_private, delete_after=10)

            # --- 處理「參數類型錯誤」 ---
            elif isinstance(error, commands.BadArgument):
                 await ctx.send(f"⚠️ **參數類型錯誤：** 您輸入的 `{error.param.name}` 格式不對。", ephemeral=is_private)
            
            else:
                # ✅ 4. 如果是本 Cog 的錯誤，但你不想處理 (例如 Cooldown)
                # 什麼都不做 (pass)，讓它自動上報給 bot.py 的全域處理器 (只會報錯一次)
                pass
        
        # ✅ 5. 絕對禁止：刪除旧的 'else' 區塊
        # 
        # (❌ 錯誤的寫法 - 必須刪除):
        # else:
        #     if self.bot.extra_events.get('on_command_error', None) is not None:
        #          await self.bot.on_command_error(ctx, error)
        #     else:
        #          logging.error(f"Unhandled error in {ctx.command}: {error}")
```

-----

## ⚠️ 注意事項 2：如何正確撰寫「Hybrid 混合指令」

我們必須**同時支援** `#` (公開) 和 `/` (私人)。

### ✅ 範例 A：簡單指令 (立即回覆)

適用於 `ping`, `list`, `queue` 等能「立即」回覆的指令。

```python
    @commands.hybrid_command(
        name="ping", 
        description="測試機器人的延遲 (ms)" # / 指令的說明
    )
    async def ping(self, ctx: commands.Context):
        """測試機器人的延遲 (ms)""" # # 指令的說明
        
        # 1. 檢查指令是如何被呼叫的
        is_private = ctx.interaction is not None

        # 2. 將 is_private 傳入 ephemeral 參數
        await ctx.send(f'{round(self.bot.latency*1000)} (ms)', ephemeral=is_private)
```

### ✅ 範例 B：帶有「可選參數」的指令

適用於 `musiclist` (頁碼可選)。

```python
    @commands.hybrid_command(
        name='musiclist', 
        aliases=['音樂清單', '清單'], 
        description="顯示已記錄的音樂清單 (使用按鈕分頁)"
    )
    # ✅ 替 / 指令的「可選參數」加上說明
    @app_commands.describe(
        page="要顯示的頁碼 (預設 1)"
    )
    async def show_music_list(self, ctx: commands.Context, page: int = 1):
        """
        顯示已記錄的音樂清單，每頁10筆，使用按鈕分頁。
        指令格式: #musiclist [頁碼]
        """
        is_private = ctx.interaction is not None
        
        # ... 執行你的程式邏輯 ...
        embed = discord.Embed(title=f"顯示第 {page} 頁")
        
        await ctx.send(embed=embed, ephemeral=is_private)
```

### ✅ 範例 C：耗時指令 (需要「思考中...」的回覆)

**這是最重要、也最複雜的模式。** 適用於 `play`, `calendar`, `importmusic` 等需要「API 請求、爬蟲、讀取檔案」的指令。

```python
    @commands.hybrid_command(
        name='addevent', 
        description="新增一個 Google 日曆活動"
    )
    @app_commands.describe(
        date_time="日期與時間 (格式: \"YYYY-MM-DD [HH:MM]\")",
        title="活動標題 (格式: \"我的標題\")"
    )
    async def add_calendar_event(self, ctx: commands.Context, date_time: str, title: str):
        """新增一個 Google 日曆活動"""

        # 1. 檢查指令是如何被呼叫的
        is_private = ctx.interaction is not None

        # 2. 發送「正在處理」訊息
        # ( / 指令會自動顯示 "Bot 正在思考...")
        # ( # 指令會發送一則 "正在新增..." 的公開訊息)
        original_message = await ctx.send(f"正在向 Google Calendar 新增活動 `{title}`...", ephemeral=is_private)

        try:
            # 3. ... (執行您的耗時任務, 例如 requests.post) ...
            await asyncio.sleep(5) # 模擬耗時
            
            # 4. 準備最終的回覆內容
            response_content = f"✅ 成功新增活動：{title}"

        except Exception as e:
            response_content = f"❌ 發生錯誤: {e}"

        # 5. 根據呼叫類型，決定如何「更新」訊息
        if is_private:
            # ✅ / 指令：必須使用 followup.send 來「編輯」思考中訊息
            await ctx.followup.send(response_content, ephemeral=True)
        else:
            # ✅ # 指令：必須使用 .edit() 來「編輯」原始的公開訊息
            await original_message.edit(content=response_content)
```

### ✅ 範例 D：指令群組 (Groups)

用於 `/monitor` 這類擁有多個子指令的功能。**這是修正 `AttributeError` 錯誤的正確方法**。

```python
    # 1. 建立主指令 (必須使用 hybrid_group)
    @commands.hybrid_group(
        name='monitor', 
        aliases=['監測', '課表監測'], 
        description="管理課程人數監測任務"
    )
    async def monitor(self, ctx: commands.Context):
        """管理課程人數監測任務。"""
        
        is_private = ctx.interaction is not None
        
        # 這是當使用者只輸入 /monitor 或 #monitor 時的回覆
        if ctx.invoked_subcommand is None:
            embed = discord.Embed(title="📚 課程人數監測管理")
            embed.add_field(name="用法", value=f"`{ctx.prefix}monitor add` 或 `/monitor add`")
            await ctx.send(embed=embed, ephemeral=is_private)

    # ✅ 2. 建立子指令 (必須使用 @monitor.command，不是 .hybrid_command)
    @monitor.command(
        name='add', 
        aliases=['新增'], 
        description="[互動式] 新增一個課程人數監測任務"
    )
    @commands.has_permissions(manage_roles=True) 
    async def add_monitor_job(self, ctx: commands.Context):
        is_private = ctx.interaction is not None
        await ctx.send("正在新增任務...", ephemeral=is_private)
        # ... (使用 followup 或 edit 來回覆結果) ...

    # ✅ 3. 建立另一個帶有參數的子指令 (必須使用 @monitor.command)
    @monitor.command(
        name='remove', 
        aliases=['移除', '刪除'], 
        description="移除一個課程人數監測任務"
    )
    @app_commands.describe(course_id="要移除的課號")
    @commands.has_permissions(manage_roles=True) 
    async def remove_monitor_job(self, ctx: commands.Context, course_id: str):
        is_private = ctx.interaction is not None
        await ctx.send(f"已移除 {course_id}", ephemeral=is_private)
```

-----

## ⚠️ 注意事項 3：【新】如何將「舊指令」升級為「新 Hybrid 指令」

這裏展示了將一個舊的、複雜的指令（如 `cmds/calendar.py`）遷移到新標準的**完整差異**。

### 1\. 遷移指令本身 (`addevent`)

```python
# ---------------------------------
# ❌ 舊的寫法 (Before)
# ---------------------------------
import discord
from discord.ext import commands
# ( ... 其他 import ... )

class Calendar(commands.Cog):
    # ...
    
    @commands.command(name='addevent', aliases=[...])
    async def add_calendar_event(self, ctx, date_time: str, title: str, duration: int = 60, calendar_key: str = "default"):
        
        # 🐛 問題點：總是公開回覆
        await ctx.send(f"正在向 Google Calendar 新增活動 `{title}`...") 

        try:
            r = await asyncio.to_thread(requests.post, ...)
            
            gas_response = r.json()
            if gas_response.get("status") == "success":
                message = gas_response.get("message")
                link = gas_response.get("link")
                
                # 🐛 問題點：又一次 ctx.send，導致多則公開回覆
                if link:
                    await ctx.send(f"{message}\n[🔗 查看日曆活動]({link})")
                else:
                    await ctx.send(f"{message}")
            else:
                await ctx.send("❌ 日曆 API 處理失敗...")
        
        except Exception as e:
            await ctx.send(f"❌ 發生程式碼錯誤: {e}")

```

```python
# ---------------------------------
# ✅ 新的寫法 (After)
# ---------------------------------
import discord
from discord.ext import commands
from discord import app_commands # ✅ 1. 引入 app_commands
# ( ... 其他 import ... )

class Calendar(Cog_Extension): # ✅ (使用你的 Cog_Extension)
    # ...
    
    @commands.hybrid_command(name='addevent', aliases=[...], description="新增一個 Google 日曆活動...") # ✅ 2. 改為 hybrid_command
    @app_commands.describe( # ✅ 3. 為 / 指令新增參數說明
        date_time="日期與時間 (格式: \"...\")",
        title="活動標題 (格式: \"...\")",
        duration="持續時間 (分鐘) (預設 60)",
        calendar_key="日曆代號 (預設 default)"
    )
    async def add_calendar_event(self, ctx: commands.Context, date_time: str, title: str, duration: int = 60, calendar_key: str = "default"):
        
        is_private = ctx.interaction is not None # ✅ 4. 檢查呼叫方式

        # ✅ 5. 發送「思考中」訊息 (並儲存它)
        original_message = await ctx.send(f"正在向 Google Calendar 新增活動 `{title}`...", ephemeral=is_private)

        try:
            r = await asyncio.to_thread(requests.post, ...)
            
            # ✅ 6. 準備「單一」的回覆內容
            response_content = ""
            if r.status_code == 200:
                gas_response = r.json()
                if gas_response.get("status") == "success":
                    message = gas_response.get("message")
                    link = gas_response.get("link")
                    # (使用三元運算子，確保 link 檢查依然存在)
                    response_content = f"{message}" + (f"\n[🔗 查看日曆活動]({link})" if link else "")
                else:
                    response_content = "❌ 日曆 API 處理失敗..."
            else:
                response_content = f"❌ 網路請求失敗：{r.status_code}"
        
        except Exception as e:
            response_content = f"❌ 發生程式碼錯誤: {e}"

        # ✅ 7. 根據呼叫方式，用「一種」方式回覆
        if is_private:
            await ctx.followup.send(response_content, ephemeral=True) # / 指令用 followup
        else:
            await original_message.edit(content=response_content) # # 指令用 edit
```

### 2\. 遷移錯誤處理 (`on_command_error`)

這是修復 `image_5d586b.png` 問題的**關鍵**。

```python
# ---------------------------------
# ❌ 舊的寫法 (Before) - 導致重複錯誤
# ---------------------------------
    @commands.Cog.listener()
    async def on_command_error(self, ctx, error):
        
        if ctx.command and ctx.command.name in ['addevent', ...]:
            
            if isinstance(error, commands.MissingRequiredArgument):
                await ctx.send(f"⚠️ 參數遺漏錯誤...")
            
        else:
            # ❌ 這裡是元兇！
            if self.bot.extra_events.get('on_command_error', None) is not None:
                 await self.bot.on_command_error(ctx, error)
```

```python
# ---------------------------------
# ✅ 新的寫法 (After) - 已修正
# ---------------------------------
    @commands.Cog.listener()
    async def on_command_error(self, ctx, error):
        
        # ✅ 1. 加入 Cog 檢查
        if ctx.command and ctx.command.cog_name != 'Calendar':
            return # 不屬於我，立刻退出

        logging.warning(f"Calendar Cog 捕獲到指令錯誤 (指令: {ctx.command}, 錯誤: {error})")

        # ✅ 2. 檢查私人回覆
        is_private = ctx.interaction is not None

        if ctx.command and ctx.command.name in ['addevent', ...]:
            
            if isinstance(error, commands.MissingRequiredArgument):
                await ctx.send(
                    f"⚠️ 參數遺漏錯誤...(範例)...", # (保留您的完整錯誤範本)
                    ephemeral=is_private # ✅ 3. 加入動態回覆
                )
            
            else:
                pass # 上報給 bot.py
        
        # ✅ 4. 刪除 'else' 區塊
```

-----

## ⚠️ 注意事項 4：【新】如何建立「讀取 JSON」的指令 (例如：FAQ 或 餐廳)

這可以讓您新增 `#food [關鍵字]` 或 `/food [關鍵字]` 指令。

### 步驟 1：建立 `data/food_list.json`

在您的 `data` 資料夾中（與 `monitor_list.json` 同一層），建立一個 `food_list.json` (一開始可以只是一個空清單 `[]`)：

```json
[
    {
        "name": "雲科後門阿姨炒飯",
        "recommender": "路人A",
        "timestamp": "2025-11-01",
        "remarks": [
            "推薦加辣 (by 路人A)",
            "老闆娘人很好 (by 路人B)"
        ]
    },
    {
        "name": "龍潭路大苑子",
        "recommender": "路人C",
        "timestamp": "2025-11-02",
        "remarks": []
    }
]
```

### 步驟 2：建立 `cmds/food.py`

在 `cmds` 資料夾中建立新檔案 `food.py`。(此範例包含 `add`, `list`, `search`, `remark`, `remove` 功能)

```python
import discord
from discord.ext import commands
from core.classes import Cog_Extension
import json
import os
import logging
from discord import app_commands
from typing import List, Dict, Any, Optional
from datetime import datetime

FOOD_FILE = './data/food_list.json'

class Food(Cog_Extension):
    
    def __init__(self, bot):
        super().__init__(bot)
        self.food_list = self._load_food_list()

    def _load_food_list(self) -> List[Dict[str, Any]]:
        """載入 food_list.json，如果不存在則建立一個空清單"""
        try:
            if os.path.exists(FOOD_FILE):
                with open(FOOD_FILE, 'r', encoding='utf8') as f:
                    return json.load(f)
            else:
                with open(FOOD_FILE, 'w', encoding='utf8') as f:
                    json.dump([], f)
                return []
        except Exception as e:
            logging.error(f"載入 {FOOD_FILE} 失敗: {e}")
            return []

    def _save_food_list(self):
        """將目前的 food_list 儲存回 JSON 檔案"""
        try:
            with open(FOOD_FILE, 'w', encoding='utf8') as f:
                json.dump(self.food_list, f, indent=4, ensure_ascii=False)
        except Exception as e:
            logging.error(f"儲存 {FOOD_FILE} 失敗: {e}")

    def _find_food(self, name: str) -> Optional[Dict[str, Any]]:
        """透過名稱尋找餐廳 (不分大小寫)"""
        self.food_list = self._load_food_list() 
        for food in self.food_list:
            if food['name'].lower() == name.lower():
                return food
        return None

    def _create_food_embed(self, food: Dict[str, Any]) -> discord.Embed:
        """建立餐廳資訊卡片 (卡片式回應)"""
        embed = discord.Embed(title=f"🍜 {food['name']}", color=discord.Color.blue())
        embed.add_field(name="最初推薦人", value=food['recommender'], inline=True)
        embed.add_field(name="推薦時間", value=food['timestamp'], inline=True)
        remark_text = "尚無備註"
        if food['remarks']:
            remark_text = ""
            for i, remark in enumerate(food['remarks']):
                remark_text += f"{i+1}. {remark}\n"
        embed.add_field(name="備註 (大家的心得)", value=remark_text, inline=False)
        return embed

    @commands.Cog.listener()
    async def on_command_error(self, ctx, error):
        if ctx.command and ctx.command.cog_name != 'Food':
            return
        logging.warning(f"Food Cog 捕獲到指令錯誤 (指令: {ctx.command}, 錯誤: {error})")
        is_private = ctx.interaction is not None
        if ctx.command and ctx.command.root_parent.name == 'food':
            if isinstance(error, commands.MissingRequiredArgument):
                await ctx.send(f"⚠️ **參數遺漏錯誤：** 您忘記提供 `{error.param.name}` 參數了！", ephemeral=is_private)
            else:
                pass # 上報

    @commands.hybrid_group(name='food', aliases=['eat', '餐廳', '吃'], description="餐廳推薦清單")
    async def food(self, ctx: commands.Context):
        is_private = ctx.interaction is not None
        if ctx.invoked_subcommand is None:
            embed = discord.Embed(title="🍜 餐廳推薦功能", description="這是一系列管理餐廳清單的指令。", color=0x4682B4)
            embed.add_field(name=f"1. 新增餐廳", value=f"`{ctx.prefix}food add [名稱] [備註(選填)]`", inline=False)
            embed.add_field(name=f"2. 查看清單", value=f"`{ctx.prefix}food list`", inline=False)
            embed.add_field(name=f"3. 搜尋餐廳", value=f"`{ctx.prefix}food search [名稱]`", inline=False)
            embed.add_field(name=f"4. 增加備註", value=f"`{ctx.prefix}food remark [名稱] [要增加的備註]`", inline=False)
            embed.add_field(name=f"5. 刪除餐廳", value=f"`{ctx.prefix}food remove [名稱]`", inline=False)
            await ctx.send(embed=embed, ephemeral=is_private)

    @food.command(name='add', aliases=['新增'], description="推薦一家新餐廳")
    @app_commands.describe(name="餐廳名稱", remark="你的備註 (選填)")
    async def add_food(self, ctx: commands.Context, name: str, *, remark: Optional[str] = None):
        is_private = ctx.interaction is not None
        if self._find_food(name):
            return await ctx.send(f"❌ 錯誤：「{name}」已經在清單中了。", ephemeral=is_private)
        remarks_list = []
        if remark:
            remarks_list.append(f"{remark} (by {ctx.author.display_name})")
        new_food = {
            "name": name,
            "recommender": ctx.author.display_name,
            "timestamp": datetime.now().strftime("%Y-%m-%d"),
            "remarks": remarks_list
        }
        self.food_list.append(new_food)
        self._save_food_list()
        await ctx.send(f"✅ 成功新增「{name}」！", embed=self._create_food_embed(new_food), ephemeral=is_private)

    @food.command(name='list', aliases=['清單'], description="列出所有已推薦的餐廳")
    async def list_food(self, ctx: commands.Context):
        is_private = ctx.interaction is not None
        self.food_list = self._load_food_list() 
        if not self.food_list:
            return await ctx.send("目前清單是空的，快使用 `/food add` 新增第一家吧！", ephemeral=is_private)
        embed = discord.Embed(title="🍜 餐廳推薦總覽", color=0x4682B4)
        all_names = [f"• **{food['name']}** (by {food['recommender']})" for food in self.food_list]
        embed.description = "\n".join(all_names)
        embed.set_footer(text=f"使用 `{ctx.prefix}food search [名稱]` 來查看詳細資訊")
        await ctx.send(embed=embed, ephemeral=is_private)

    @food.command(name='search', aliases=['find', '查'], description="查詢特定餐廳的詳細資訊")
    @app_commands.describe(name="要查詢的餐廳名稱")
    async def search_food(self, ctx: commands.Context, name: str):
        is_private = ctx.interaction is not None
        food_entry = self._find_food(name)
        if food_entry:
            await ctx.send(embed=self._create_food_embed(food_entry), ephemeral=is_private)
        else:
            await ctx.send(f"❌ 找不到名稱為「{name}」的餐廳。", ephemeral=is_private)

    @food.command(name='remark', aliases=['備註', 'addnote'], description="為已存在的餐廳新增備註")
    @app_commands.describe(name="餐廳名稱", text="要新增的備註/心得")
    async def add_remark(self, ctx: commands.Context, name: str, *, text: str):
        is_private = ctx.interaction is not None
        food_entry = self._find_food(name)
        if not food_entry:
            return await ctx.send(f"❌ 找不到名稱為「{name}」的餐廳，無法新增備註。", ephemeral=is_private)
        remark_text = f"{text} (by {ctx.author.display_name})"
        food_entry['remarks'].append(remark_text)
        self._save_food_list()
        await ctx.send(f"✅ 已為「{food_entry['name']}」新增備註！", embed=self._create_food_embed(food_entry), ephemeral=is_private)

    @food.command(name='remove', aliases=['delete', '刪除'], description="刪除一家餐廳推薦")
    @app_commands.describe(name="要刪除的餐廳名稱")
    async def remove_food(self, ctx: commands.Context, name: str):
        is_private = ctx.interaction is not None
        food_entry = self._find_food(name)
        if not food_entry:
            return await ctx.send(f"❌ 找不到名稱為「{name}」的餐廳。", ephemeral=is_private)
        self.food_list.remove(food_entry)
        self._save_food_list()
        await ctx.send(f"🗑️ 已刪除「{name}」。 (操作者: {ctx.author.display_name})", ephemeral=is_private)

async def setup(bot):
    await bot.add_cog(Food(bot))
```

-----

## ⚠️ 注意事項 5：【新】如何建立「關鍵字自動回應」(無須 \#)

這可以讓機器人在偵測到特定訊息（如 "你好"）時自動回覆。

### 步驟 1：確認 `bot.py` 的 `Intents`

您的 `bot.py` 必須啟用 `message_content`。
**您目前的 `intents = discord.Intents.all()` 已經包含了這項權限，所以您不需要修改 `bot.py`。**

### 步驟 2：建立 `cmds/listener.py`

在 `cmds` 資料夾中建立新檔案 `listener.py`。

```python
import discord
from discord.ext import commands
from core.classes import Cog_Extension
import logging
import random # (範例 3 需要)

class Listener(Cog_Extension):

    @commands.Cog.listener()
    async def on_message(self, msg: discord.Message):
        
        # 1. 檢查是否為機器人本身，防止無限循環
        if msg.author == self.bot.user:
            return
            
        # 2. 檢查是否在 DM (私人訊息) 中 (可選)
        if isinstance(msg.channel, discord.DMChannel):
            return

        # 3. 檢查訊息內容
        content = msg.content.lower() # 轉換為小寫

        # --- 範例 1：完全符合 (回覆文字) ---
        if content == "你好":
            await msg.channel.send("你好啊！")
            return # (回覆後就可以退出了)

        # --- 範例 2：包含關鍵字 (回覆「標準」表情符號) ---
        if "早安" in content:
            try:
                await msg.add_reaction("☀️") # 加上太陽表情符號
            except discord.Forbidden:
                logging.warning(f"無法在頻道 {msg.channel.name} 新增表情符號 (權限不足)")
            return

        # --- 範例 3：包含關鍵字 (隨機回覆文字) ---
        if "好棒" in content or "太強了" in content:
            responses = ["謝謝誇獎！", "還好啦 (´∀`)", "低調低調～"]
            await msg.channel.send(random.choice(responses))
            return

        # --- 範例 4：完全符合 (回覆圖片 URL) ---
        if content == "貼圖":
            image_url = "https://i.imgur.com/86n43L8.gif" # 範例貼圖
            await msg.channel.send(image_url)
            return
            
        # ✅ --- 範例 5：(您詢問的) 回覆「本機圖片」---
        # 
        # Docker 如何打包：
        # 1. 您的 Dockerfile 中有 `COPY . .` 指令。
        # 2. 這會將您 GitHub 儲存庫中的「所有檔案」都複製到 Docker 映像檔的 `/app/` 目錄中。
        # 
        # 圖片要放哪裡：
        # 1. 在您的專案根目錄（與 bot.py 同層）建立一個資料夾，例如 `data/`。
        # 2. 將您的圖片 (例如 `my_image.png`) 放入該資料夾。
        # 3. 路徑會是： `./data/my_image.png`
        #
        if content == "本機圖片":
            try:
                # Docker 容器內的路徑會是 /app/data/my_image.png
                # 因為您的 WORKDIR 是 /app，所以相對路徑是 './data/my_image.png'
                await msg.channel.send(file=discord.File('./data/my_image.png'))
            except FileNotFoundError:
                await msg.channel.send("❌ 抱歉，找不到本機圖片檔案。")
                logging.error("在 on_message 中找不到 './data/my_image.png' 檔案。")
            return

        # ✅ --- 範例 6：(您詢問的) 回覆「自訂表情符號」---
        #
        # 如何取得 ID：
        # 1. 在 Discord 頻道中輸入 \:您要的表情符號: (例如 \:my_emoji:)
        # 2. Discord 會回傳 <a:my_emoji:123456789012345678> (動圖)
        #    或 <:my_emoji:123456789012345678> (靜態)
        # 3. 把「完整的字串」複製下來
        #
        if "想睡覺" in content:
            # --- 方法 A：作為「訊息」回覆 ---
            emoji_string = "<:my_sleep_emoji:123456789>" 
            await msg.channel.send(emoji_string)
            return

        if "拍手" in content:
            # --- 方法 B：作為「反應」新增 ---
            reaction_emoji_string = "<:my_clap_emoji:987654321>"
            try:
                await msg.add_reaction(reaction_emoji_string)
            except (discord.Forbidden, discord.HTTPException):
                logging.warning(f"無法新增自訂表情符號 {reaction_emoji_string} (權限不足或找不到)")
            return

        # 4. ❌ 絕對禁止 ❌
        # 不要在 Cog 的 on_message 中使用 process_commands
        # (錯誤的寫法 - 必須刪除):
        # await self.bot.process_commands(msg)

async def setup(bot):
    await bot.add_cog(Listener(bot))
```